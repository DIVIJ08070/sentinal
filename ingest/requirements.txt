# Sentinel ingest — default (lightweight) dependency set.
# Heavy ML deps (torch/ultralytics/fast-plate-ocr) are deliberately kept out of
# this file per docs/CONTRACT.md quality bars — see requirements-ml.txt.
httpx>=0.27,<1.0
numpy>=1.26,<3.0
opencv-python-headless>=4.10,<5.0
